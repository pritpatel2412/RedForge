{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:api": "cross-env PORT=8080 dotenv -e .env -- pnpm --filter @workspace/api-server run dev",
    "dev:web": "cross-env PORT=5173 BASE_PATH=/ dotenv -e .env -- pnpm --filter @workspace/redforge run dev",
    "dev": "npx concurrently --kill-others \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db run push",
    "db:seed": "dotenv -e .env -- pnpm --filter @workspace/scripts run seed",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.1",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
